<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for ParkFlow Manager.

  - Enforces HTTPS for all connections in release builds.
  - Allows cleartext (HTTP) only in debug builds for local development.
  - Pins the server certificate for the production API domain.

  Reference: https://developer.android.com/privacy-and-security/security-config
-->
<network-security-config>
    <!-- Default: no cleartext traffic allowed -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Production API domain with certificate pinning -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.parkflow.example.com</domain>
        <pin-set expiration="2027-01-01">
            <!--
              Replace with your actual certificate SHA-256 pins.
              Generate with:
                openssl s_client -servername api.parkflow.example.com \
                  -connect api.parkflow.example.com:443 \
                  | openssl x509 -pubkey -noout \
                  | openssl pkey -pubin -outform der \
                  | openssl dgst -sha256 -binary \
                  | openssl enc -base64
            -->
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            <!-- Backup pin (different CA or key) -->
            <pin digest="SHA-256">BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB=</pin>
        </pin-set>
    </domain-config>

    <!-- Allow cleartext for local development (debug only) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
